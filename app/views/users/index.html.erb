<% provide(:title, 'All users') %> 
<h1>All users</h1>
<h1>There are currently <%= @users.count %> users</h1>

<%= will_paginate %>

<ul class="users">
	<% @users.each do |user| %>
		<%= render @users %>
	<% end %> 
</ul>

<%= will_paginate %>